'use strict';

describe('Server Module', function () => {
  describe('#Routes', function () {
    it('400, it should respond with \'bad request\' if no id was provided in the request', function () {
      expect(Router.prototype.route().toBe())
    })

  })
})